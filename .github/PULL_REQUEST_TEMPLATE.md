## Summary

<!-- Brief description of what this PR changes and why. -->

## Type of change

- [ ] CLI / dashboard (released via `v*` tags → npm)
- [ ] Team-server (released via `server-v*` tags → GHCR)
- [ ] Docs / tooling only

## Test plan

- [ ] `pnpm test` passes
- [ ] `pnpm typecheck` passes

## CHANGELOG

- [ ] Added a CHANGELOG entry for this change (required for any user-facing change — the release gate hard-fails without it)
  - CLI/dashboard → root `CHANGELOG.md`
  - Team-server → `packages/team-server/CHANGELOG.md`

<!-- For user-facing changes, add an entry under a `## [<version>] — YYYY-MM-DD` heading matching the next planned release. -->
