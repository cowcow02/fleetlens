POSTGRES_PASSWORD=change-me-to-a-random-secret
BASE_URL=https://fleetlens.yourdomain.com
DOMAIN=fleetlens.yourdomain.com
# Required to store GitHub/Linear/Jira/email integration credentials at rest.
# AES-256-GCM key: 64 hex characters (32 bytes). Generate with:
#   openssl rand -hex 32
FLEETLENS_ENCRYPTION_KEY=
